import '../TakeRecording/TakeRecording.css';

import React from 'react';

export default class TakeRecording extends React.Component{
    render(){
        return(
            <button>Take Recording</button>
        )
    }
}